"""
Program Name: Octal to Decimal Conversion

Problem:
Given an octal number as a string, convert it to its decimal equivalent.

Example

Input:
octal = "17"

Output:
15  
"""

class Solution:
    def octalToDecimal(self, octal: str) -> int:
        decimal = 0
        
        # Convert octal to decimal
        
        return decimal