"""
Program Name: Hexadecimal to Decimal Conversion

Problem:
Given a hexadecimal number as a string, convert it to its decimal equivalent.

Example

Input:
hexadecimal = "1A"

Output:
26
"""

class Solution:
    def hexadecimalToDecimal(self, hexadecimal: str) -> int:
        decimal = 0
        
        # Convert hexadecimal to decimal
        
        return decimal