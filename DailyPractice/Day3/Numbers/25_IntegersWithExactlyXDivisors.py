"""
Program Name: Find Number of Integers Which Have Exactly X Divisors

Problem:
Given n and x, return the count of integers from 1 to n that have exactly x divisors.

Example

Input:
n = 10, x = 4

Output:
3

Explanation:
The numbers are 6, 8, 10.
"""

class Solution:
    def countNumbersWithXDivisors(self, n: int, x: int) -> int:
        count = 0
        
        for num in range(1, n + 1):
            divisors = 0
            
            # Count divisors of num
            
            # Check if divisors == x
            
            pass
        
        return count