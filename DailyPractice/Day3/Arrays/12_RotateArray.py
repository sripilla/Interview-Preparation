"""
Program Name: Rotate an Array Left and Right

Problem:

Given an array and k, rotate the array left or right by k positions.

Example

Input:

nums = [1, 2, 3, 4, 5]
k = 2

Output:

Right rotation = [4, 5, 1, 2, 3]
"""

class Solution:
    def rotate(self, nums: list[int], k: int) -> None:
        # Normalize k
        
        # Think about reversal / juggling / slicing
        
        # Rotate in-place if required
        
        pass