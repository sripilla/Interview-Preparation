"""
Program Name: Move All Negative Elements to One Side

Problem:

Given an array of positive and negative integers, move all negative elements to one side of the array.

Example

Input:

nums = [1, -2, 3, -4, 5, -6]

Output:

[-2, -4, -6, 1, 3, 5]
"""

class Solution:
    def moveNegatives(self, nums: list[int]) -> list[int]:
        # Use two pointers / partitioning
        
        # Move negative elements to one side
        
        pass