"""
Program Name: Find Subarray with Sum Equal to Zero

Problem:

Given an integer array, determine whether there exists a contiguous subarray whose sum is zero.

Example

Input:

nums = [4, 2, -3, 1, 6]

Output:

True
"""

class Solution:
    def zeroSumSubarray(self, nums: list[int]) -> bool:
        # Maintain a running prefix sum
        
        # Store previously seen prefix sums
        
        # If a sum repeats, zero-sum subarray exists
        
        pass