"""
Program Name: Toggle Case of Each Character

Problem:

Given a string s, convert uppercase characters to lowercase and lowercase characters to uppercase.

Example

Input:

s = "HeLLo"

Output:

"hEllO"
"""

class Solution:
    def toggleCase(self, s: str) -> str:
        result = []
        
        for ch in s:
            # If uppercase, convert to lowercase
            
            # If lowercase, convert to uppercase
            
            # Otherwise keep unchanged
            
            pass
        
        return "".join(result)