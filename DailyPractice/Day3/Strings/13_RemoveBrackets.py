"""
Program Name: Remove Brackets from an Algebraic Expression

Problem:

Given an algebraic expression containing brackets, remove the brackets while correctly handling the signs.

Example

Input:

s = "a-(b+c)"

Output:

"a-b-c"
"""

class Solution:
    def removeBrackets(self, s: str) -> str:
        result = []
        stack = []
        
        # Track the sign before the current bracket
        
        current_sign = 1
        
        for ch in s:
            # If character is an operand
            
            # If '+' or '-'
            
            # If '('
            
            # If ')'
            
            pass
        
        return "".join(result)