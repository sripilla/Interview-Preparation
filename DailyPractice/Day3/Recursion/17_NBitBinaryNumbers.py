"""
Program Name: N-Bit Binary Numbers Having More 1's Than 0's

Problem:
Given an integer n, generate all n-bit binary numbers in which the number of 1s is greater than or equal to the number of 0s at every prefix.

Example

Input:
n = 3

Output:

["111", "110", "101"]
"""

class Solution:
    def generateBinary(self, n: int) -> list[str]:
        result = []
        
        def generate(current, ones, zeros):
            # Base case
            
            # Add 1 when allowed
            
            # Add 0 when allowed
            
            pass
        
        generate("", 0, 0)
        return result