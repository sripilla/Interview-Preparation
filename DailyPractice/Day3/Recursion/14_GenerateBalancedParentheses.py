"""
Program Name: Generate All Combinations of Well-Formed Parentheses

Problem:
Given an integer n representing the number of pairs of parentheses, generate all combinations of well-formed parentheses.

Example

Input:
n = 3

Output:

["((()))", "(()())", "(())()", "()(())", "()()()"]
"""

class Solution:
    def generateParenthesis(self, n: int) -> list[str]:
        result = []
        
        def backtrack(current, open_count, close_count):
            # Base case
            
            # Add an opening parenthesis
            
            # Add a closing parenthesis
            
            pass
        
        backtrack("", 0, 0)
        return result