"""
Program Name: Sum of All Subsets

Problem:
Given an array of N integers, generate the sums of all possible subsets.

Example

Input:
arr = [1, 2, 3]

Output:
[0, 1, 2, 3, 3, 4, 5, 6]
"""

class Solution:
    def subsetSums(self, arr: list[int]) -> list[int]:
        result = []
        
        def generate(index, current_sum):
            # Base case
            
            # Include current element
            
            # Exclude current element
            
            pass
        
        generate(0, 0)
        return result